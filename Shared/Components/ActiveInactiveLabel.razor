@if (IsActive)
{
    <MudChip T="string" Label="true" Color="Color.Success">Active</MudChip>
}
else
{
    <MudChip T="string" Label="true" Color="Color.Default">Inactive</MudChip>
}

@code {
    [Parameter]
    public bool IsActive { get; set; }
}
