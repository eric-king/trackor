@if (IsActive)
{
    <MudChip Label="true" Color="Color.Success">Active</MudChip>
}
else
{
    <MudChip Label="true" Color="Color.Default">Inactive</MudChip>
}

@code {
    [Parameter]
    public bool IsActive { get; set; }
}
